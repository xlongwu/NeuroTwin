# NeuroTwin 开发协作规范

## 适用范围

本文件约束本项目的日常开发、调试、测试、文档和协作方式，适用于所有直接修改本仓库文件的开发者与自动化代理。

本规范**不规定模型优化或模型结构调整**。除非用户明确提出，禁止根据本文件自行修改网络结构、损失函数、训练目标、数据增强策略、优化器、学习率、批大小或其他会改变模型效果的实验设置。

## 开发前

- 先阅读与当前任务直接相关的代码、配置和文档，确认现有行为后再修改。
- 先检查工作区状态，保留用户已有的未提交改动，不覆盖、不回退与当前任务无关的修改。
- 明确改动范围和验收标准；优先采用最小、局部、可回滚的改动。
- 修改前确认入口、配置来源、输入输出格式和依赖关系，避免仅凭文件名或注释推断行为。

## 代码修改

- 遵循现有项目的目录组织、命名风格和调用方式；不要为了个人偏好进行无关重构。
- 保持已有公共接口、命令行参数、配置字段、文件格式和返回值兼容；确需变更时同步更新调用方和文档。
- 新增函数或类应有清晰的职责、合理的参数名和必要的类型标注；避免过长函数、隐式全局状态和重复实现。
- 不把路径、设备、随机种子、数据集位置等环境相关值硬编码到业务逻辑中；优先使用现有配置或命令行参数。
- 错误处理应保留足够上下文。不要使用空的 `except`，也不要用静默兜底掩盖数据、路径或配置错误。
- 日志应说明关键上下文，避免在循环中无必要地大量打印；禁止提交密钥、令牌、个人数据或大体积运行日志。
- 仅在能改善维护性的地方添加注释；注释解释原因和约束，不重复显而易见的代码。
- 生成的临时文件、缓存、调试输出和本地环境配置不得纳入版本控制。

## 数据与实验安全

- 不修改、删除或覆盖 `checkpoints/`、原始数据和用户生成的结果，除非用户明确授权且目标已确认。
- 调试和验证优先使用小规模、只读或临时输出；不要默认启动长时间训练或批量生成结果。
- 涉及随机过程时，沿用项目已有的随机种子和设备设置；新增随机逻辑应明确记录其来源。
- 任何会影响实验结果的改动，都应在说明中列出影响范围、运行命令和可复现条件。
- 不将本地绝对路径、机器专属配置或个人凭据写入代码、配置和文档。

## 测试与验证

- 修改后至少执行与改动直接相关的静态检查、导入检查或最小运行验证。
- 修复缺陷时优先补充能复现问题的测试；新增功能应覆盖正常路径和主要异常路径。
- 测试应尽量确定性、可重复，并避免依赖外部服务、个人目录或未声明的本地状态。
- 若完整测试因数据、GPU、依赖或运行时间无法执行，应执行可行的替代验证，并在交付说明中明确未验证部分及原因。
- 不因让测试“通过”而删除断言、放宽校验、屏蔽错误或改变与任务无关的行为。

## 文档与配置

- 用户可见行为、命令行参数、配置字段、目录结构或运行步骤发生变化时，更新对应文档。
- 文档中的命令、路径和示例应与当前项目实际情况一致；不要保留已失效的说明。
- 配置示例不得包含真实密钥、账号信息或不可公开的数据路径。
- 分析结论应区分事实、推断和待验证假设，避免把一次实验结果表述为普遍结论。

## Git 与交付

- 提交应聚焦单一目的，提交信息简洁说明做了什么以及必要时说明原因。
- 不提交 IDE 配置、缓存、临时文件、模型权重、运行日志或其他未经确认的大文件。
- 交付前检查变更列表和差异，确认没有误改、调试代码、敏感信息或无关格式化。
- 交付说明应包含：改动摘要、验证命令及结果、已知限制，以及需要用户后续决定的事项。
- 未经明确要求，不执行破坏性操作（如批量删除、覆盖数据、重置工作区或强制改写历史）。

## 代理行为边界

- 用户要求“修复”时只修复已确认的问题；不要顺带进行模型调参、结构优化或性能重写。
- 用户要求“分析”时先提供证据和结论，不自动修改代码或实验配置。
- 对可能造成数据丢失、运行成本显著增加或改变实验结论的操作，应先确认范围和目标。
- 遇到不确定的项目约定时，优先从现有代码和文档中查证，并在无法确认时明确说明假设。
